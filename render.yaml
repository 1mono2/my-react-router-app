services:
  - type: web
    name: my-react-router-app
    runtime: node
    buildCommand: npm ci && npx react-router build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    # Render will automatically set PORT environment variable
